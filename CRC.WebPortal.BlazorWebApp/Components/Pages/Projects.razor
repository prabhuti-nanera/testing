@page "/projects"
@using Microsoft.AspNetCore.Components
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect to home page which shows the projects list
        Navigation.NavigateTo("/home", replace: true);
    }
}
